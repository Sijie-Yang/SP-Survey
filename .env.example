# ── Local dev ports (optional) ───────────────────────────────────────────────
PORT=3021
CLIENT_ORIGIN=http://localhost:3020

# ── React dev server (create .env.local for CRA) ─────────────────────────────
# PORT=3020
# REACT_APP_API_URL=http://localhost:3021

# ── Supabase (optional — can also configure per-project in Admin UI) ─────────
# Used for survey response storage when running deployed surveys.
# REACT_APP_SUPABASE_URL=https://your-project.supabase.co
# REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key

# Per-project Supabase credentials (service role + anon) are saved in the
# Admin → Step 1 Image Dataset → Supabase Storage Configuration panel.

# ── AI Assistant ─────────────────────────────────────────────────────────────
# Users enter their OpenAI or OpenRouter key in the app. No server key required.
